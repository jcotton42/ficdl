<Window x:Class="FicDl.Pages.DownloadProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:local="clr-namespace:FicDl.Pages"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:DownloadProgressViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <ProgressBar
            Minimum="0"
            Value="{Binding CurrentChapterNumber}"
            Maximum="{Binding ChapterCount}"
            IsIndeterminate="{Binding IsIndeterminate}"
            Height="20"/>
        <Button
            Content="Cancel"
            Command="{s:Action CancelDownload}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            MinHeight="20"
            MinWidth="50"/>
    </StackPanel>
</Window>
